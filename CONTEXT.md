# 版本线钉死

actdsh 把上游 DeepSeek Harness 的发布版本原样打包成桌面应用。「版本线钉死」是这个上下文的名称：它负责让安装包里的 dsh 家族包版本与上游某个 release 完全一致，并把实际装进去的版本如实报告给用户。

## Language

### 上游与家族

**上游**：
deepseek-ai/deepseek-harness 及其发布到 npm 的包。actdsh 不修改它，只重新打包。
_Avoid_: 官方源、原项目

**dsh 家族包**：
参与钉死闭包的、`@deepseek-ai/` 作用域下的 npm 包。同版本线包与独立版本线包都属于家族包。
_Avoid_: 内部包、自有包

**上游 tag 版本**：
一次上游 release 对应的版本号，是钉死的基准。家族包按是否发布过这个版本号分成下面两类。

### 两类家族包

**同版本线包**：
发布过上游 tag 版本的家族包。整棵树上必须全部等于该版本，不允许任何副本偏离。
_Avoid_: 钉死包、主版本包

**独立版本线包**：
没有发布过上游 tag 版本、按自己节奏发版的家族包。它的版本由依赖它的家族包所声明的区间决定，而不是由 npm 上的最新版本决定。
_Avoid_: 第三方包、外部包、浮动包

**peer 补齐包**：
只通过非可选的 peer 依赖可达、因而必须被显式声明的家族包。属于独立版本线包的一个子集。
_Avoid_: 缺失的 peer、隐式依赖

### 纯度与副本

**release 纯度**：
安装包内的家族包版本与上游该 release 所声明的版本一致这一不变量。它约束的是实际被加载的版本，不是被声明的版本。
_Avoid_: 版本一致性、锁定

**顶层副本**：
安装在依赖树根部的那一份包。构建版本清单报告的必须是它，因为它是解析的落点。
_Avoid_: 主副本

**嵌套副本**：
安装在某个依赖方自己子树里的那一份包，出现在依赖方要求的版本与顶层不同时。它是包管理器的正常行为，不是纯度缺陷。
_Avoid_: 重复安装、脏副本

### 产物

**构建版本清单**：
一次构建实际装进去的版本记录，会原样出现在面向用户的发布说明里。因此它必须是观测结果，不能是意图。
_Avoid_: 版本报告、锁文件

**壳**：
actdsh 自己编写并维护的 Electron 外壳，负责窗口、就绪信号与插件垫片。它不是上游产物。
_Avoid_: wrapper、宿主

**用户视角验证**：
发布前对安装产物的真实启动验收：装、启、等就绪、访问界面、走通插件链路。任一项不过则不发布。
_Avoid_: 冒烟测试、健康检查
